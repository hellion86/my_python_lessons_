*написать функцию которая выбрасывает одно из трех исключений
valueError
TypeError
runtimeError случайным образом.
в месте вызова функции обрабатывать все три исключения

*написать функцию которая принимает на воод список если в списке
все объекты int - сортировать его, иначе выдать ValueError

--написать функцю которая принимает словарь преобразует все ключи 
словаря к строкам и выводит новый словарь

*написать функцию которая принимает список чисел и возвращает их произведение
